name: Build Flutter Android APK

on:
  # 手动触发
  workflow_dispatch:
    inputs:
      app_version:
        description: '应用版本号 (例如: 1.0.0)'
        required: false
        default: '1.0.0'
        type: string
      build_type:
        description: '构建类型'
        required: true
        default: 'apk-split'
        type: choice
        options:
          - apk-split    # 按 ABI 分包（推荐，体积小）
          - apk-fat      # 合并单包（兼容所有设备）
          - both         # 同时构建两种
      sign_apk:
        description: '是否使用正式签名（需提前配置 4 个 Secrets）'
        required: true
        default: 'false'
        type: choice
        options:
          - 'false'      # debug 签名，无需任何配置，直接可用
          - 'true'       # 正式签名，需配置 Secrets（见 README）

env:
  FLUTTER_VERSION: '3.32.0'  # ← 修改为你使用的 Flutter 版本
  APP_NAME: 'secretchat'     # ← 修改为 pubspec.yaml 中的 name
  JAVA_VERSION: '17'         # Flutter 3.x 需要 Java 17

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      # 1. 检出代码
      - name: 检出代码
        uses: actions/checkout@v4

      # 2. 设置 Java
      - name: 设置 Java ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}

      # 3. 设置 Flutter
      - name: 设置 Flutter ${{ env.FLUTTER_VERSION }}
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      # 4. 配置 Gradle（移除本地 Java home 设置）
      - name: 配置 Gradle
        run: |
          sed -i '/org.gradle.java.home/d' android/gradle.properties || true

      # 5. 安装依赖
      - name: 安装 Flutter 依赖
        run: flutter pub get

      # 6. 生成应用图标
      - name: 生成应用图标
        run: dart run flutter_launcher_icons

      # 7. 配置正式签名（仅当手动选择 sign_apk=true 时执行）
      - name: 配置签名 Keystore
        if: ${{ github.event.inputs.sign_apk == 'true' }}
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/keystore.jks

          cat > android/key.properties << EOF
          storePassword=${{ secrets.KEYSTORE_PASSWORD }}
          keyPassword=${{ secrets.KEY_PASSWORD }}
          keyAlias=${{ secrets.KEY_ALIAS }}
          storeFile=keystore.jks
          EOF
          echo "✅ 正式签名配置完成"

      # 8a. 构建分包 APK（按 ABI：arm64-v8a / armeabi-v7a / x86_64）
      - name: 构建分包 APK
        if: ${{ github.event.inputs.build_type == 'apk-split' || github.event.inputs.build_type == 'both' || github.event.inputs.build_type == '' }}
        run: |
          flutter build apk --release --split-per-abi
          echo "✅ 分包 APK 构建完成"
          ls -lh build/app/outputs/flutter-apk/

      # 8b. 构建合并 Fat APK
      - name: 构建 Fat APK
        if: ${{ github.event.inputs.build_type == 'apk-fat' || github.event.inputs.build_type == 'both' }}
        run: |
          flutter build apk --release
          echo "✅ Fat APK 构建完成"
          ls -lh build/app/outputs/flutter-apk/

      # 9. 重命名并整理产物
      - name: 整理 APK 产物
        run: |
          VERSION="${{ github.event.inputs.app_version || '1.0.0' }}"
          APP="${{ env.APP_NAME }}"
          SIGN="${{ github.event.inputs.sign_apk }}"
          SRC="build/app/outputs/flutter-apk"
          DIST="dist"
          mkdir -p "$DIST"

          for apk in "$SRC"/*.apk; do
            [ -f "$apk" ] || continue
            filename=$(basename "$apk")
            newname="${filename/app-/${APP}-${VERSION}-}"
            cp "$apk" "$DIST/$newname"
            echo "📦 $newname ($(du -sh "$DIST/$newname" | cut -f1))"
          done

          echo "签名方式: $([ "$SIGN" = 'true' ] && echo '正式签名' || echo 'debug 签名')"
          echo "--- 最终产物 ---"
          ls -lh "$DIST"

      # 10. 上传 Artifact
      - name: 上传 APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-android-apk
          path: dist/*.apk
          retention-days: 30

  # ─────────────────────────────────────────
  # 创建 GitHub Release
  # ─────────────────────────────────────────
  release:
    needs: build-android
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch' && github.event.inputs.app_version != ''
    permissions:
      contents: write

    steps:
      - name: 下载构建产物
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: 查看产物
        run: find artifacts/ -type f | sort

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.event.inputs.app_version }}
          name: Release v${{ github.event.inputs.app_version }}
          draft: false
          prerelease: false
          generate_release_notes: true
          files: artifacts/**/*.apk
          body: |
            ## 📱 Android 下载

            | 文件 | 适用设备 |
            |------|----------|
            | `*arm64-v8a-release.apk` | 主流现代手机（64位，**推荐**） |
            | `*armeabi-v7a-release.apk` | 旧款手机（32位） |
            | `*x86_64-release.apk` | 模拟器 / x86 设备 |
            | `*fat-release.apk` | 通用包（兼容所有设备，体积较大） |

            > 不确定选哪个？下载 `arm64-v8a` 版本，覆盖 95% 以上现代 Android 设备。

            ## 🚀 安装说明

            1. 下载对应的 `.apk` 文件
            2. 手机设置 → 安全 → 允许安装未知来源应用
            3. 点击 APK 文件安装
